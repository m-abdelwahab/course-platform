import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Course Platform</title>
        <meta name="description" content="Course Platform template" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>Hell world</main>
    </div>
  );
};

export default Home;
